# semantic-diff-ir

# semantic-diff-ir

This project compares LLVM IR differences between two C programs
and aims to build a semantic-aware diff tool.

## Steps
1. Compile C → LLVM IR
2. Compare IR differences
3. Analyze semantic impact
